const TextComponent = require('./text-component');

describe('text component tests', () => {

  it('Renders proper data to text node or returns empty string')
  
  it('updates data on a text node')

  it('returns same TextNode on second render')

  

});